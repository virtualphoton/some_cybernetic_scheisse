# some cybernetic scheisse

Required packages are in requirements.txt

Run `simulation/main.py` for simulation and `server/app.py` for main server

These scripts probably won't fall when another one is stopped